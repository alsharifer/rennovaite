# lib/overlays

Reserved for **Pilot Seven — Prompt P3 (3D room overlays)**.

Empty scaffold created by the pre-flight (see `PILOT_SEVEN_PREFLIGHT.md`).
Feature-flagged by `OVERLAYS_ENABLED`. NB: `three` / `@react-three/fiber` /
`@react-three/drei` are NOT yet installed — P3 adds them.
