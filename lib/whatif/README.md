# lib/whatif

Reserved for **Pilot Seven — Prompt P4 (What-if / `element_refs`)**.

Empty scaffold created by the pre-flight (see `PILOT_SEVEN_PREFLIGHT.md`).
Feature-flagged by `WHATIF_ENABLED`. BoQ lines live inside the `boqs.sections`
jsonb blob, so `element_refs` attach *inside each line item*, not as a column.
